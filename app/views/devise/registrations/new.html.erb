
<%= render "shared/banner" %>

<!--   <h2 class="content">Create your account</h2> -->
    <div class="form-container">

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= f.error_notification %>

  <div class="form-wrapper-signup">
    <%= f.input :first_name,
                required: true,
                autofocus: true %>
    <%= f.input :last_name,
                required: true,
                autofocus: true %>
    <%= f.input :city,
                required: true,
                autofocus: true %>
    <%= f.input :email,
                required: true,
                autofocus: true ,
                input_html: { autocomplete: "email" }%>
    <%= f.hidden_field :buddy, :value => false %>
    <% if params[:buddy] %>
      <%= f.input :plz,
                  class: "form-control",
                  placeholder: "Enter your postcode"%>
      <% @specialties = ["Anmeldung", "Bank Account", "Tax Advise" ] %>

      <%= f.select "specialties", options_for_select(@specialties, "specialties"),
                    include_blank: true,
                    class: "form-control",
                    prompt: "Select a procedure" %>
      <%= f.hidden_field :buddy, :value => true %>
    <% end %>
    <%= f.input :password,
                required: true,
                hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                input_html: { autocomplete: "new-password" } %>
    <%= f.input :password_confirmation,
                required: true,
                input_html: { autocomplete: "new-password" } %>


    <div class="form-actions">
        <%= f.button :submit, "Sign up", class: "submit-button" %>
      </div>
      <div class="link">
        <%= render "devise/shared/links" %>
      </div>
      </div>

      <% end %>

    </div>
  </div>
</div>
</div>
